<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Nbc_sns" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="android:background">@color/white</item>
        <!-- statusBarColor = 앱 상태 색깔 변경 -->
        <item name="android:statusBarColor">@color/white</item>

        <item name="windowNoTitle">true</item>
        <!-- statusBarColor = 앱 상대 아이콘 색깔 변경 true = black, false = white -->
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.Nbc_sns" parent="Base.Theme.Nbc_sns" />

</resources>